# 레이아웃 리팩토링 계획

> 작성일: 2024-12-09  
> 버전: 1.0  
> 상태: 계획 단계

---

## 1. 개요

### 1.1 목적

현재 단일 파일에 집중된 로직을 분리하고, CAD 스타일의 3-Column 레이아웃으로 UI를 개선한다.

### 1.2 주요 목표

- [ ] FloorPlanCanvas.vue (2000줄+) 분할
- [ ] Pinia Store로 상태 관리 분리
- [ ] Option A (클래식 CAD 스타일) 레이아웃 적용
- [ ] 코드 유지보수성 향상

---

## 2. 현재 vs 목표 레이아웃

### 2.1 현재 구조

```
┌────────────────────────┐
│ 헤더 (저장/내보내기)    │
├────┬───────────────────┤
│Act │ 사이드패널        │
│Bar │ (가구/레이어/...) │
│    │                   │
│    │   캔버스          │
│    │                   │
└────┴───────────────────┘
```

### 2.2 목표 구조 (Option A)

```
┌──────────────────────────────────────────────────────────┐
│ 메뉴바 (파일/편집/보기 드롭다운)           [저장] [내보내기] │
├────────┬─────────────────────────────────┬───────────────┤
│ 도구   │                                 │ 속성 패널     │
│ 바     │                                 │ ─────────────│
│────────│                                 │ X: [  120  ] │
│ 라이브 │          메인 캔버스             │ Y: [  340  ] │
│ 러리   │                                 │ W: [  180  ] │
│        │                                 │ H: [  200  ] │
│ ┌────┐ │                                 │───────────────│
│ │침대│ │                                 │ 레이어 패널   │
│ └────┘ │                                 │ ─────────────│
│ ┌────┐ │─────────────────────────────────│ 👁 ☑ 배경    │
│ │소파│ │ [−] 100% [+]  |  X: 120 Y: 340  │ 👁 ☑ 방      │
└────────┴─────────────────────────────────┴───────────────┘
   200px          flex: 1 (가변)              240px
```

---

## 3. 컴포넌트 구조 변경

### 3.1 현재 구조

```
app/components/
├── editor/
│   ├── FloorPlanCanvas.vue  ← 2000줄+ (문제)
│   ├── DoorEditForm.vue
│   ├── FurnitureEditForm.vue
│   ├── LayerPanel.vue
│   └── WallEditForm.vue
├── layout/
│   ├── ActivityBar.vue
│   └── ActivityBarItem.vue
└── sidebar/
    └── FurnitureLibrary.vue
```

### 3.2 목표 구조

```
app/components/
├── canvas/
│   ├── FloorPlanCanvas.vue      # 메인 (조합만, ~200줄)
│   ├── CanvasStage.vue          # Konva Stage 래퍼
│   ├── RoomLayer.vue            # 방 렌더링
│   ├── FurnitureLayer.vue       # 가구 렌더링
│   ├── WallLayer.vue            # 벽체 렌더링
│   ├── DoorLayer.vue            # 문 렌더링
│   ├── MeasureLayer.vue         # 측정 오버레이
│   └── TransformerLayer.vue     # 선택/변환
├── menubar/
│   ├── MenuBar.vue              # 상단 메뉴바
│   ├── MenuDropdown.vue         # 드롭다운 메뉴
│   └── MenuItem.vue             # 메뉴 아이템
├── panel/
│   ├── LeftPanel.vue            # 좌측 패널 컨테이너
│   ├── RightPanel.vue           # 우측 패널 컨테이너
│   ├── PropertyPanel.vue        # 속성 편집 패널
│   └── LayerPanel.vue           # 레이어 패널 (이동)
├── toolbar/
│   ├── ToolBar.vue              # 도구 버튼 그룹
│   ├── ToolButton.vue           # 개별 도구 버튼
│   └── FurnitureLibrary.vue     # 가구 라이브러리 (이동)
├── statusbar/
│   └── StatusBar.vue            # 하단 상태바
├── modal/
│   ├── RoomModal.vue            # 방 생성 모달
│   ├── DoorModal.vue            # 문 추가 모달
│   └── ConfirmModal.vue         # 확인 모달
└── form/
    ├── DoorEditForm.vue         # 문 편집 (이동)
    ├── FurnitureEditForm.vue    # 가구 편집 (이동)
    └── WallEditForm.vue         # 벽 편집 (이동)
```

---

## 4. 상태 관리 분리 (Pinia Store)

### 4.1 현재 상태

모든 상태가 `FloorPlanCanvas.vue`의 `ref()`로 관리됨:

```typescript
// FloorPlanCanvas.vue 내부 (현재)
const room = ref<Room | null>(null)
const furnitureList = ref<Furniture[]>([])
const wallList = ref<Wall[]>([])
const selectedFurniture = ref<Furniture | null>(null)
const isMeasureMode = ref(false)
// ... 50개 이상의 ref
```

### 4.2 목표 구조

```
app/stores/
├── useEditorStore.ts       # 에디터 전역 상태
├── useCanvasStore.ts       # 캔버스 데이터
├── useSelectionStore.ts    # 선택 상태
└── useHistoryStore.ts      # Undo/Redo
```

#### useEditorStore.ts

```typescript
export const useEditorStore = defineStore('editor', () => {
  // 활성 도구
  const activeTool = ref<ToolType>('select')
  type ToolType = 'select' | 'room' | 'wall' | 'door' | 'furniture' | 'measure' | 'image'

  // 뷰 상태
  const zoom = ref(1)
  const panX = ref(0)
  const panY = ref(0)

  // 모드
  const mode = ref<ModeType>('normal')
  type ModeType = 'normal' | 'draw' | 'measure'

  // UI 상태
  const showGrid = ref(true)
  const showRuler = ref(false)
  const snapEnabled = ref(true)

  return { activeTool, zoom, panX, panY, mode, showGrid, showRuler, snapEnabled }
})
```

#### useCanvasStore.ts

```typescript
export const useCanvasStore = defineStore('canvas', () => {
  // 데이터
  const rooms = ref<Room[]>([])
  const furnitureList = ref<Furniture[]>([])
  const wallList = ref<Wall[]>([])
  const doorList = ref<Door[]>([])
  const floorPlanImage = ref<FloorPlanImage | null>(null)
  const groups = ref<ObjectGroup[]>([])

  // 첫 번째 방 (호환성)
  const room = computed({
    get: () => rooms.value[0] ?? null,
    set: (val) => { /* ... */ }
  })

  // CRUD 액션
  function addFurniture(furniture: Furniture) { /* ... */ }
  function removeFurniture(id: string) { /* ... */ }
  function updateFurniture(id: string, data: Partial<Furniture>) { /* ... */ }
  // ...

  return { rooms, room, furnitureList, wallList, doorList, floorPlanImage, groups, /* actions */ }
})
```

#### useSelectionStore.ts

```typescript
export const useSelectionStore = defineStore('selection', () => {
  // 단일 선택
  const selectedId = ref<string | null>(null)
  const selectedType = ref<ObjectType | null>(null)
  type ObjectType = 'room' | 'furniture' | 'wall' | 'door' | 'image' | 'group'

  // 다중 선택
  const multiSelectedIds = ref<Set<string>>(new Set())

  // 호버
  const hoveredId = ref<string | null>(null)

  // Computed
  const hasSelection = computed(() => selectedId.value !== null)
  const isMultiSelect = computed(() => multiSelectedIds.value.size > 1)

  // 액션
  function select(id: string, type: ObjectType) { /* ... */ }
  function toggleMultiSelect(id: string) { /* ... */ }
  function clearSelection() { /* ... */ }

  return { selectedId, selectedType, multiSelectedIds, hoveredId, hasSelection, isMultiSelect, /* actions */ }
})
```

#### useHistoryStore.ts

```typescript
export const useHistoryStore = defineStore('history', () => {
  const undoStack = ref<CanvasState[]>([])
  const redoStack = ref<CanvasState[]>([])
  const maxHistory = 50

  const canUndo = computed(() => undoStack.value.length > 0)
  const canRedo = computed(() => redoStack.value.length > 0)

  function pushState(state: CanvasState) { /* ... */ }
  function undo(): CanvasState | null { /* ... */ }
  function redo(): CanvasState | null { /* ... */ }

  return { canUndo, canRedo, pushState, undo, redo }
})
```

---

## 5. 페이지 레이아웃 변경

### 5.1 layouts/default.vue (단순화)

```vue
<template>
  <div class="h-screen flex flex-col">
    <slot />
  </div>
</template>
```

### 5.2 pages/index.vue (새 구조)

```vue
<template>
  <div class="h-screen flex flex-col bg-gray-100">
    <!-- 상단 메뉴바 -->
    <MenuBar class="h-12 shrink-0" />
    
    <!-- 메인 영역 -->
    <div class="flex-1 flex overflow-hidden">
      <!-- 좌측: 도구 + 라이브러리 -->
      <LeftPanel class="w-[200px] shrink-0" />
      
      <!-- 중앙: 캔버스 + 상태바 -->
      <main class="flex-1 flex flex-col">
        <FloorPlanCanvas class="flex-1" />
        <StatusBar class="h-8 shrink-0" />
      </main>
      
      <!-- 우측: 속성 + 레이어 -->
      <RightPanel class="w-[240px] shrink-0" />
    </div>
  </div>
</template>
```

---

## 6. 작업 단계 (Phases)

### Phase 1: Pinia Store 생성 (우선순위: 높음)

| 작업 | 예상 시간 | 상태 |
|------|----------|------|
| useEditorStore.ts 생성 | 1h | ⬜ |
| useCanvasStore.ts 생성 | 1.5h | ⬜ |
| useSelectionStore.ts 생성 | 0.5h | ⬜ |
| useHistoryStore.ts 생성 (기존 composable 이동) | 0.5h | ⬜ |
| Store 단위 테스트 작성 | 1h | ⬜ |

### Phase 2: FloorPlanCanvas 분할

| 작업 | 예상 시간 | 상태 |
|------|----------|------|
| CanvasStage.vue 분리 | 1h | ⬜ |
| RoomLayer.vue 분리 | 0.5h | ⬜ |
| FurnitureLayer.vue 분리 | 1h | ⬜ |
| WallLayer.vue 분리 | 1h | ⬜ |
| DoorLayer.vue 분리 | 0.5h | ⬜ |
| MeasureLayer.vue 분리 | 0.5h | ⬜ |
| TransformerLayer.vue 분리 | 0.5h | ⬜ |

### Phase 3: 새 UI 컴포넌트

| 작업 | 예상 시간 | 상태 |
|------|----------|------|
| MenuBar.vue + MenuDropdown.vue | 1.5h | ⬜ |
| LeftPanel.vue (도구바 + 라이브러리) | 1.5h | ⬜ |
| RightPanel.vue (속성 + 레이어) | 1.5h | ⬜ |
| PropertyPanel.vue | 1h | ⬜ |
| StatusBar.vue | 0.5h | ⬜ |

### Phase 4: 페이지 재구성

| 작업 | 예상 시간 | 상태 |
|------|----------|------|
| layouts/default.vue 단순화 | 0.25h | ⬜ |
| pages/index.vue 재구성 | 1h | ⬜ |
| 기존 컴포넌트 삭제/정리 | 0.5h | ⬜ |

### Phase 5: 테스트 & 버그 수정

| 작업 | 예상 시간 | 상태 |
|------|----------|------|
| E2E 셀렉터 업데이트 | 1h | ⬜ |
| Store 테스트 추가 | 1h | ⬜ |
| 통합 테스트 실행 | 1h | ⬜ |
| 버그 수정 | 2h | ⬜ |

---

## 7. 파일 변경 요약

### 7.1 삭제 예정

```
app/components/
├── layout/
│   ├── ActivityBar.vue       # LeftPanel로 대체
│   └── ActivityBarItem.vue   # ToolButton으로 대체
```

### 7.2 이동 예정

| 현재 위치 | 새 위치 |
|----------|---------|
| `editor/LayerPanel.vue` | `panel/LayerPanel.vue` |
| `sidebar/FurnitureLibrary.vue` | `toolbar/FurnitureLibrary.vue` |
| `editor/DoorEditForm.vue` | `form/DoorEditForm.vue` |
| `editor/FurnitureEditForm.vue` | `form/FurnitureEditForm.vue` |
| `editor/WallEditForm.vue` | `form/WallEditForm.vue` |

### 7.3 신규 생성

```
app/
├── stores/
│   ├── useEditorStore.ts      # 신규
│   ├── useCanvasStore.ts      # 신규
│   ├── useSelectionStore.ts   # 신규
│   └── useHistoryStore.ts     # 신규
├── components/
│   ├── canvas/
│   │   ├── CanvasStage.vue    # 신규
│   │   ├── RoomLayer.vue      # 신규
│   │   ├── FurnitureLayer.vue # 신규
│   │   ├── WallLayer.vue      # 신규
│   │   ├── DoorLayer.vue      # 신규
│   │   ├── MeasureLayer.vue   # 신규
│   │   └── TransformerLayer.vue # 신규
│   ├── menubar/
│   │   ├── MenuBar.vue        # 신규
│   │   ├── MenuDropdown.vue   # 신규
│   │   └── MenuItem.vue       # 신규
│   ├── panel/
│   │   ├── LeftPanel.vue      # 신규
│   │   ├── RightPanel.vue     # 신규
│   │   └── PropertyPanel.vue  # 신규
│   ├── toolbar/
│   │   ├── ToolBar.vue        # 신규
│   │   └── ToolButton.vue     # 신규
│   ├── statusbar/
│   │   └── StatusBar.vue      # 신규
│   └── modal/
│       ├── RoomModal.vue      # 신규
│       ├── DoorModal.vue      # 신규
│       └── ConfirmModal.vue   # 신규
```

---

## 8. 예상 일정

| Phase | 작업 | 예상 시간 | 누적 |
|-------|------|----------|------|
| 1 | Pinia Store | 4.5h | 4.5h |
| 2 | Canvas 분할 | 5h | 9.5h |
| 3 | 새 UI 컴포넌트 | 6h | 15.5h |
| 4 | 페이지 재구성 | 1.75h | 17.25h |
| 5 | 테스트 & 버그 수정 | 5h | 22.25h |
| | **총계** | **~23h** | |

---

## 9. 리스크 및 대응

| 리스크 | 영향도 | 대응 방안 |
|--------|--------|----------|
| Konva 이벤트 전파 문제 | 높음 | Layer별 이벤트 핸들러 분리 |
| Store 반응성 끊김 | 높음 | storeToRefs 사용, 테스트 강화 |
| E2E 테스트 실패 | 중간 | data-testid 미리 추가 |
| 작업 중 기능 손실 | 높음 | Phase별 브랜치 관리, 롤백 계획 |

---

## 10. 성공 기준

- [ ] 모든 기존 기능 정상 동작
- [ ] E2E 테스트 100% 통과
- [ ] Unit 테스트 100% 통과
- [ ] FloorPlanCanvas.vue 300줄 이하
- [ ] 각 Store 테스트 커버리지 80% 이상
- [ ] 새 레이아웃 반응형 동작 (최소 1024px)

---

## 부록: 참고 자료

- [Pinia 공식 문서](https://pinia.vuejs.org/)
- [vue-konva 레이어 관리](https://konvajs.org/docs/vue/)
- [Nuxt 3 레이아웃](https://nuxt.com/docs/guide/directory-structure/layouts)
